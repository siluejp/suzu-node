<!doctype html>
<html ng-app="todoApp">
<head>
  <title>Strom Fall wiki</title>
  <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.5.6/angular.min.js"></script>
  <script src="/js/app.js"></script>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>
<body>

  <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/stormfall">Stormfall wiki</a>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="active"><a href="/stormfall">Home</a></li>
          <li><a href="#about">同盟募集板</a></li>
          <li><a href="#about">Tips集（裏技？）</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </nav>

  <div class="container" style="padding: 40px 15px;">

    <div class="starter-template">
      <h1>戦力分析 <small>Battlegrounds Analytics</small></h1>
      <p class="lead">戦場（攻撃）の敵戦力を分析して最適な攻撃ユニットを提案します。</p>
    </div>

    <div ng-controller="TodoListController as todoList">
      <div class="row">
        <table class="table">
          <tr>
            <th>兵数</th>
            <th>兵種</th>
            <th>名前</th>
            <th>防御力（最大）</th>
            <th>割合</th>
          </tr>
          <tr ng-repeat="todo in todoList.todos">
            <td><input ng-change="todoList.calc()" type="number" ng-model="todo.ammount" size="10"></td>
            <td>{{todo.type}}</td>
            <td><img class="img-rounded" ng-src="{{todo.img_url}}" alt="Description" width="20px" /><span style="padding-left: 10px;">{{todo.name}}</span></td>
            <td>{{todo.defense * todo.ammount}}（{{todo.max_defense * todo.ammount}}）</td>
            <td>{{todo.per | number:2}}</td>
          </tr>
        </table>
        <div ng-if="todoList.has_outputs()">
          <blockquote>
            <div class="row" ng-repeat="todo in todoList.todos" >
              <div class="col-md-12">{{todo.type}}のみ: {{todo.output | number}}</div>
            </div>
            <div class="row">
              <div class="col-md-12">平均：{{todoList.avg_defense() | number}}</div>
            </div>
          </blockquote>
          <small>※その兵種で攻撃した場合の相手の戦力です</small>
        </div>
      </div>
    </div>

  </div><!-- /.container -->
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
</body>
</html>
